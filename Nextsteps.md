1. Create a manual vendor file name mapping for the #file:generate_vendor_directory.py. Review the vendor directory #file:2026_vendor_directory.html  for any vendors missing descriptions, match them, where appropriate, to existing vendors in #file:_posts 
2. Change PPL First from 61 to 63
3. Update the services and age groups categories to look like tags (with distinct colors)
4. "Empty" is not a real vendor
5. Vendors that have cancelled should be added to the bottom of the directory (label it with a polite, max 3 word way of saying they are not present):Luke5Adventures, Carrita Counseling, Keck Health, DVB Financial and Technology Assisted Children’s Home Program (TACHP)
6. The indexes should be 2 columned lists, and do not need the "vendor" word (that is implied)
7. Use the fonts and styles from the website to create the vendor directory
8. Remove the Index by Service Category, only keep the age category
9. The index should contain the vendors that have cancelled